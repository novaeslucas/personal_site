import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/theme-provider";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-jetbrains-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Lucas | Desenvolvedor de Software & Gerente de Projetos",
  description:
    "Portfólio de Lucas — Desenvolvedor Backend Java com experiência em Spring Boot, Kafka, Docker e Gestão de Projetos. Atualmente contribuindo no TCM-BA.",
  openGraph: {
    title: "Lucas | Desenvolvedor de Software",
    description:
      "Portfólio profissional com projetos e experiências em desenvolvimento backend e gestão de projetos.",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Lucas | Desenvolvedor de Software",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-br" suppressHydrationWarning>
      <head>
        {/* Material Symbols */}
        <link
          href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
          rel="stylesheet"
        />
      </head>
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange={false}
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
